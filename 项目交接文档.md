# 星河生日祝福项目交接文档

更新时间：2026-08-17

## 项目是什么

这是一个生日祝福互动网站，线上项目名是「星河生日祝福」。网站核心流程是：

1. 打开首页，点击开启惊喜。
2. 播放 5、4、3、2、1 粒子倒计时。
3. 显示「生日快乐」粒子文字和烟花。
4. 展示三层生日蛋糕。
5. 通过按钮或手势展开为照片星球。
6. 在照片星球中可旋转、查看照片、播放背景音乐。
7. 可在定制面板里设置名字、祝福语和最多 10 张照片，并生成礼物分享链接。

项目已经接入真正的 MediaPipe HandLandmarker 手势识别，不再使用前后帧运动检测冒充手势。

## 当前本机目录

当前代码目录：

```bash
/Users/a221209/Documents/New project/ 生日祝福手势
```

注意：文件夹名开头有一个空格，实际名字是 ` 生日祝福手势`。

同级目录里的 `Particle-Flow-Squash-main` 是之前下载的参考项目，只用于参考 MediaPipe 手势和粒子交互实现，不是当前线上网站源码。

## 线上信息

Sites 项目名称：

```text
星河生日祝福
```

Sites slug：

```text
stellar-birthday-wish
```

线上地址：

```text
https://stellar-birthday-wish.lakesky-sq.chatgpt.site
```

Sites project_id：

```text
appgprj_6a65851f83888191ad38e8923d899a82
```

当前已知线上发布版本：

```text
Version 7
```

Version 7 对应本地提交：

```text
0fdb487 Tune gesture selection controls
```

`.openai/hosting.json` 当前配置：

```json
{
  "d1": "DB",
  "project_id": "appgprj_6a65851f83888191ad38e8923d899a82",
  "r2": "GIFT_MEDIA"
}
```

重要：迁移到另一台电脑后不要创建新的 Sites 项目。保留 `.openai/hosting.json`，Codex/Sites 会继续识别并使用原项目。

## Git 远程

当前 GitHub 远程：

```bash
git@github.com:Bai1623/birhday.git
```

当前 Sites 源码远程：

```bash
https://git.chatgpt-team.site/862d8a24-e16e-46d8-8c33-333a183db09a/appgprj_6a65851f83888191ad38e8923d899a82.git
```

本机远程名：

```text
github -> GitHub 仓库
origin -> Sites 源码仓库
```

在新电脑上优先从 GitHub 克隆即可，Sites 源码仓库需要 Codex/Sites 的临时凭证才能推送。

## 新电脑迁移步骤

推荐方式：从 GitHub 克隆。

```bash
git clone git@github.com:Bai1623/birhday.git birthday-wish
cd birthday-wish
npm install
```

如果你直接拷贝当前文件夹到新电脑，也可以。建议不要拷贝这些目录：

```text
node_modules
dist
.wrangler
.sites-runtime
.next
```

拷贝后进入项目目录重新安装依赖：

```bash
npm install
```

Node.js 要求：

```text
>= 22.13.0
```

## 本地运行

开发预览：

```bash
npm run dev
```

默认会启动 Vite/Vinext 开发服务。终端会显示本地地址，通常类似：

```text
http://localhost:5173
```

如果端口被占用，可以指定端口：

```bash
npm run dev -- --host 127.0.0.1 --port 5180
```

## 测试和构建

Lint：

```bash
npm run lint
```

类型检查：

```bash
npx tsc --noEmit
```

完整测试和构建：

```bash
npm test
```

构建：

```bash
npm run build
```

`npm test` 会运行手势识别、MediaPipe 资源、礼物分享、页面行为测试，然后执行构建和 HTML 检查。

## 部署方式

在 Codex 里继续使用 Sites 工具部署。不要手动新建项目。

常规流程：

1. 修改代码。
2. 运行 `npm run lint`、`npx tsc --noEmit`、`npm test`。
3. 提交到 Git。
4. 推送 GitHub。
5. 使用 Sites 工具获取源码仓库临时写入凭证。
6. 推送到 Sites 源码仓库。
7. 打包 `dist`。
8. 保存新的 Sites version。
9. 发布该 version 到原线上地址。

之前发布 Version 7 的线上地址仍是：

```text
https://stellar-birthday-wish.lakesky-sq.chatgpt.site
```

## 主要代码结构

```text
app/page.tsx
```

主页面和交互流程，包括倒计时、蛋糕、照片星球、定制面板、相机启动、手势事件处理。

```text
app/lib/gesture-core.js
```

MediaPipe 21 点关键点后的手势分类逻辑，包括：

- 张开手掌
- 握拳
- 捏合
- 食指点选
- EMA 平滑
- 连续帧确认和冷却时间

```text
app/lib/hand-orbit-control.js
```

手掌移动控制照片星球旋转。当前旋转速度已经调慢，`HAND_YAW_DELTA_GAIN = 8.2`。

```text
app/lib/particle-scene.ts
```

Three.js 粒子渲染、倒计时文字、生日快乐文字、蛋糕形态、星球形态、烟花。

```text
app/lib/gift-config.ts
```

礼物分享配置、名字/祝福/照片数据清洗、分享参数处理。

```text
app/api/gifts
```

礼物分享 API。使用 D1 保存礼物配置，使用 R2 保存上传照片。

```text
db/schema.ts
drizzle/0000_robust_cargill.sql
```

D1 数据库 schema 和迁移。

```text
public/mediapipe
```

本地 MediaPipe HandLandmarker 模型和 WASM 文件。不要删除，否则线上手势识别会失效。

## 当前手势规则

蛋糕界面：

- 自动尝试开启相机手势控制。
- 张开手掌：展开照片星球。
- 相机不可用或拒绝授权时，按钮仍然可用。

照片星球界面：

- 左右移动手掌：控制左右旋转。
- 上下移动手掌：控制俯仰角。
- 食指点选：选中或收回当前照片。
- 捏合：选中或收回当前照片。
- 握拳：收起照片星球，回到生日蛋糕。
- 鼠标拖动和手机触屏拖动仍然可用。

## 最近关键版本记录

```text
0fdb487 Tune gesture selection controls
```

Version 7。调慢手势旋转速度，新增食指点选作为照片选择手势，优化捏合和握拳误判。

```text
3b1eb3c Start hand tracking on cake stage
```

动画结束进入生日蛋糕界面后自动尝试初始化相机，张开手掌可直接展开照片星球。

```text
6e6c76e Improve hand orbit gesture sensitivity
```

优化左右挥手旋转逻辑，支持同方向连续挥动累积旋转。

```text
c182ee1 Fix gallery close and expand gift photos
```

修复收拢生日蛋糕，照片数量扩展到 10 张，展开星球时自动尝试相机。

```text
ec63fa5 Add durable gift sharing links
```

接入 D1/R2 礼物分享链接，支持定制内容通过链接发送给对方。

## 迁移时要注意

1. 不要把 `Particle-Flow-Squash-main` 当成当前项目。
2. 不要删除 `public/mediapipe`。
3. 不要删除 `.openai/hosting.json`，它绑定原 Sites 项目。
4. 不要提交 `node_modules`、`dist`、`.wrangler`、`.sites-runtime`。
5. 当前目录里的 Word 文档 `第二块专利说明书_最终(2).docx` 已按要求排除，不属于此网站项目。
6. 如果新电脑 Codex 无法直接发布 Sites，先确认已登录同一个 ChatGPT/Codex 账号，并且 Sites 插件可用。

## 快速检查清单

迁移到新电脑后，按顺序执行：

```bash
cd birthday-wish
npm install
npm run lint
npx tsc --noEmit
npm test
npm run dev
```

本地打开页面后检查：

1. 首页能打开。
2. 点击开启惊喜后有 5 到 1 倒计时。
3. 最后出现生日蛋糕。
4. 到蛋糕界面时右上角会尝试开启手势。
5. 点击展开回忆星球可以看到 10 张照片。
6. 鼠标或触屏拖动能旋转照片星球。
7. 线上发布时仍发布到原地址。
